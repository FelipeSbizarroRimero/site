let input = require("readline-sync");

let x = input.questionInt("Digite um numero: ");

while (x > 20) {
}

let y = x;

while (y >= 1) {
    if (y >= 1) {
        console.log("valor = " + y);
    }
    y--;
}